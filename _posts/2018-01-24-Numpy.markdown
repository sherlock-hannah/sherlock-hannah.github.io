---
layout: post
title:  "Numpy"
date:   2018-01-24 18:43:31 +0800
categories: jekyll update
---
numpy is nD array structure
- 1.numpy array contain only one type,if contain different types,the boolean and the float were both convert to strings.`import array from numpy` use `array()`directly
- 2.2D Numpy Arrays
`shape`show x row y column
`[n,:]` this will select n row completely
- 3.Basic Statistics
`mean()` how to select column

`median()`
`std()`standand deviation
`corrcoef`check if x,y are correlated
- 4.[numpy](http://www.numpy.org/)

How to array two column
`np.column_stack()`
